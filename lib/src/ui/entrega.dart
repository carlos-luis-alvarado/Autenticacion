import 'package:flutter/material.dart';

class entrega extends StatelessWidget {
  static Widget create(BuildContext context) => entrega();
  const entrega({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
